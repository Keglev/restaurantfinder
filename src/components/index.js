// This file is an index for exporting components from the 'components' folder.
// It allows other files to import multiple components easily from one location.

// Export the default export from 'ImageCard' and rename it to 'Card'.
export { default as Card } from './ImageCard';

// Export the default export from 'RestaurantCard'.
export { default as RestaurantCard } from './RestaurantCard';

// Export the default export from 'Modal'.
export { default as Modal } from './Modal';

// Export the default export from 'Map'.
export { default as Map } from './Map';

// Export the default export from 'loader'.
export { default as Loader } from './loader';

// Export the default export from 'Skeleton', which is the loading skeleton component.
export { default as Skeleton } from './Skeleton';

